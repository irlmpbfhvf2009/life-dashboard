# 台股飆股雷達 - Python 依賴套件
#
# 使用「版本下限」而非死鎖（==），讓 pip 在不同 Python 版本上
# 都能抓到對應的預編譯 wheel，避免在新版 Python（如 3.14）退回
# 原始碼編譯而需要 C/C++ 編譯器。
#
# 已驗證可用於 Python 3.11（GitHub Actions）與 3.14（本機）。
yfinance>=0.2.51
pandas>=2.2.3
numpy>=1.26.0
tqdm>=4.66.0
requests>=2.31.0
anthropic>=0.40.0
# pytrends：注意力面向(Google Trends)實驗訊號用，僅本機手動跑 trends.py 需要。
# CI 不裝也不跑（避免機房 IP 被 Google 限流，並維持 Actions 全免費）。
pytrends>=4.9.0
